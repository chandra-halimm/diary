import React from "react";
import Wrapper from "@/components/global/Wrapper";

const page = (): React.ReactElement => {
  return (
    <React.Fragment>
      <Wrapper>
        <h1>My Diary</h1>
      </Wrapper>
    </React.Fragment>
  );
};

export default page;
